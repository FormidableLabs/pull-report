{{#each this~}}
* {{org}}:
{{~#each repos}}
  * {{name}}: ({{items.length}}){{#each items}}
    * {{#if assignee}}{{assignee}}{{else}}[unassigned]{{/if}} / {{user}} - {{number}}: {{title}}{{#if url}}
      {{url}}{{/if}}{{/each}}
{{/each}}
{{/each~}}
